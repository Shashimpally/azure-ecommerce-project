name: Backend CI/CD

on:
  push:
    branches: [ "main" ]
    paths:
      - "backend/**"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to ACR
        uses: azure/docker-login@v2
        with:
          login-server: acrecomt5.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push image
        run: |
          cd backend
          docker build -t acrecomt5.azurecr.io/ecom-backend:${{ github.sha }} .
          docker push acrecomt5.azurecr.io/ecom-backend:${{ github.sha }}

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: rg-ecom-dev-t5
          cluster-name: aks-ecom-dev

      - name: Deploy to AKS
        run: |
          sed -i "s#acrecomt5.azurecr.io/ecom-backend:v1#acrecomt5.azurecr.io/ecom-backend:${{ github.sha }}#g" infra/backend-deployment.yaml
          kubectl apply -f infra/backend-deployment.yaml
